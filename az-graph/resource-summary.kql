// Count of resources by type
// Author: rdoherty@microsoft.com
// Updated: 16-July-2020
  
resources
| project
    id,
    resCategory = tostring(split(type, "/")[0]),
    resType = tostring(split(type, "/")[1])
| summarize 
    resCount = count() by resCategory, resType
| order by 
	resCategory asc,
	resType asc
